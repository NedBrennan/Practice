function oppositeSide(address, n) {
    //add = address
    //streetNums = []

    //for (i = 0; i < n; i ++) {
    //    streetNums.push([1 + (i * 2), (n * 2) - (2 * i)])
    //}

    return (((n * 2) + 1) - address)
}

// Learned to think about the base mathematics
// Learned not to overcomplicate a problem
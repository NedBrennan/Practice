function rot13(message){
    // turn character to array
    messageArray = message.split('')
    newMessage = ''
    // check if character is letter
    
    // if character is letter shift 13 over
    // if position > 26 ? pos - 26
  }
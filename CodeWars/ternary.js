// This is the Ternary operator
// it operates like an if else statement without
// the need for the words

firstName = 'John'
age = 16

age >= 18 ? console.log(firstName + ' drinks a beer.') : console.log(firstName + ' drinks juice.');

var drink = age >= 18 ? 'beer' : 'juice'
console.log(drink);var job = 'teacher'
switch (job) {
